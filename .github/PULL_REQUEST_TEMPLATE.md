## What does this PR do?

<!-- A short summary. Link related issues with "Fixes #123". -->

## How was it tested?

- [ ] `npm test` passes
- [ ] Verified manually with `npm run open:mac` or `npm run scan` (if the change affects runtime behavior)

## Checklist

- [ ] No raw session logs, prompt text, tokens, or private paths are included in this PR
- [ ] The change keeps CodexLens local-only and read-only
- [ ] UI strings are added for all three languages (en / ja / zh) if applicable
