## What does this PR do?

<!-- A short summary. Link related issues with "Fixes #123". -->

## How was it tested?

- [ ] `npm test` passes
- [ ] Manually verified in the menu bar app (`npm run open:mac`) where relevant

## Checklist

- [ ] No raw session logs, prompt text, tokens, or private paths are included
- [ ] The change keeps CodexLens local-only and read-only (no telemetry, no writes to observed files)
- [ ] UI strings are added for all three languages (en / ja / zh) if applicable
